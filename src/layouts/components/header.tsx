import type { FC } from 'react';

export const AppHeader: FC = () => {
    return (
        <header>
            <div>Logo</div>
            <div>breadcrumbs</div>
            <div>user menus</div>
        </header>
    );
};
